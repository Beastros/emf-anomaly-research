# Archive

This folder contains non-core scripts and wrappers moved out of the repository root to keep the share surface clean.

- `legacy-top-level/`: historical one-off analysis scripts, batch wrappers, and ad-hoc data files previously in the root.

Core runnable paths remain at root (`sniffer.py`, `phoenix_run_models.py`, `investigate_ticks.py`) and in `core/`, `lenses/`, and `events/`.
